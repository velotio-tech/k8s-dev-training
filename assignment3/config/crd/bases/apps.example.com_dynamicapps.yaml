---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: dynamicapps.apps.example.com
spec:
  group: apps.example.com
  names:
    kind: DynamicApp
    listKind: DynamicAppList
    plural: dynamicapps
    singular: dynamicapp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DynamicApp is the Schema for the dynamicapps API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DynamicAppSpec defines the desired state of DynamicApp
            properties:
              replicas:
                default: 1
                description: Replicas for demo workload
                format: int32
                minimum: 1
                type: integer
              targetResources:
                description: TargetResources holds GVK targets to create and manage
                items:
                  description: ResourceTarget defines a dynamic GVK target resource
                    to manage
                  properties:
                    group:
                      description: Group is the API group (e.g., "", "apps")
                      type: string
                    kind:
                      description: Kind is the resource kind (e.g., "ConfigMap", "Service")
                      type: string
                    name:
                      description: Name is the name of the child resource
                      type: string
                    version:
                      description: Version is the API version (e.g., "v1")
                      type: string
                  required:
                  - group
                  - kind
                  - name
                  - version
                  type: object
                type: array
            required:
            - replicas
            type: object
          status:
            description: DynamicAppStatus defines the observed state of DynamicApp
            properties:
              managedResources:
                description: ManagedResources lists formatted strings of created child
                  resources
                items:
                  type: string
                type: array
              phase:
                description: Phase of controller management
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
