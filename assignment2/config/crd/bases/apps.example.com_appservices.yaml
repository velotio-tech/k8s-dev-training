---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: appservices.apps.example.com
spec:
  group: apps.example.com
  names:
    kind: AppService
    listKind: AppServiceList
    plural: appservices
    singular: appservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AppService is the Schema for the appservices API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AppServiceSpec defines the desired state of AppService
            properties:
              environment:
                description: Environment specifies deployment tier (dev, staging,
                  prod).
                enum:
                - dev
                - staging
                - prod
                type: string
              image:
                description: Image is the container image to run.
                minLength: 5
                type: string
              ports:
                description: Ports is a list of internal container ports (1 to 5 items).
                items:
                  format: int32
                  type: integer
                maxItems: 5
                minItems: 1
                type: array
              replicas:
                default: 1
                description: Replicas is the desired number of instances (1 to 10).
                format: int32
                maximum: 10
                minimum: 1
                type: integer
            required:
            - environment
            - image
            - replicas
            type: object
          status:
            description: AppServiceStatus defines the observed state of AppService
            properties:
              activeEndpoints:
                description: ActiveEndpoints lists reachable IP addresses.
                items:
                  type: string
                type: array
              lastSyncTime:
                description: LastSyncTime records the last controller reconciliation
                  timestamp.
                format: date-time
                type: string
              phase:
                default: Pending
                description: Phase tracks lifecycle state (Pending, Running, Failed).
                enum:
                - Pending
                - Running
                - Failed
                type: string
              readyReplicas:
                description: ReadyReplicas represents current active instances.
                format: int32
                minimum: 0
                type: integer
            required:
            - phase
            - readyReplicas
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
