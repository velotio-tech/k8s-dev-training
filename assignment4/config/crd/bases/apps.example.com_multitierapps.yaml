---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: multitierapps.apps.example.com
spec:
  group: apps.example.com
  names:
    kind: MultiTierApp
    listKind: MultiTierAppList
    plural: multitierapps
    singular: multitierapp
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MultiTierAppSpec defines the desired state of MultiTierApp
            properties:
              jobCommand:
                description: JobCommand is the script/command Level 3 Job will execute
                type: string
              replicas:
                default: 1
                description: Replicas for Level 2 Deployment
                format: int32
                minimum: 1
                type: integer
              targetNamespace:
                description: TargetNamespace restricts management to a single namespace
                type: string
            required:
            - jobCommand
            - replicas
            - targetNamespace
            type: object
          status:
            description: MultiTierAppStatus defines the observed state of MultiTierApp
            properties:
              deploymentName:
                type: string
              jobName:
                type: string
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
